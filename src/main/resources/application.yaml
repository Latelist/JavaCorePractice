server:
  port: 8189
  servlet:
    context-path: /app
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:jdbcpracticedb;MODE=PostgreSQL;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: salad
    password:
  security:
    user:
      name: user
      password: pass
  h2:
    console:
      enabled: true
logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: TRACE

